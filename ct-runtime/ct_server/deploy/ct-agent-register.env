# Environment overrides for CT agent register service
# Copy to /etc/default/ct-agent-register or use systemd EnvironmentFile directive
ALLOWLIST_PATH=/opt/coordination-layer/ct-runtime/ct_server/allowlist.json
REGISTRATIONS_AUDIT=/var/log/ct-agent/registrations.log
ACCEPTED_REGISTRATIONS=/var/log/ct-agent/accepted.jsonl
